{
    "name": "database",
    "private": true,
    "type": "module",
    "dependencies": {
        "@neondatabase/serverless": "^1.0.0",
        "drizzle-kit": "^0.30.6",
        "drizzle-orm": "^0.41.0"
    },
    "devDependencies": {
        "dotenv": "^16.4.5"
    },
    "scripts": {
        "db:generate-migrations": "drizzle-kit generate",
        "db:migrate": "drizzle-kit push",
        "tsc": "tsc --noEmit"
    }
}
